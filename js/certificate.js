document.addEventListener('DOMContentLoaded',()=>{const user=EcoStore.get('currentUser',{name:'Cristian Arboleda'});$('#certificateName').textContent=user.name;$('#certificateDate').textContent=new Intl.DateTimeFormat('es-CO',{day:'numeric',month:'long',year:'numeric'}).format(new Date());$('#printCertificate').addEventListener('click',()=>window.print());$('#shareCertificate').addEventListener('click',async()=>{const text=`He completado el curso Clima en transformación en EcoAula.`;if(navigator.share){await navigator.share({title:'Mi certificado EcoAula',text})}else{await navigator.clipboard.writeText(text);showToast('Texto para compartir copiado') }})});

